[project]
name = "apr"
version = "0.1.0"
description = "Agent-first local service registry and persistent HTTP/API port allocator"
readme = "README.md"
requires-python = ">=3.12"
license = { text = "MIT" }
authors = [{ name = "APR Contributors" }]
dependencies = [
    "typer>=0.15.0",
    "starlette>=0.45.0",
    "uvicorn>=0.34.0",
    "pydantic>=2.10.0",
    "httpx>=0.28.0",
    "pyyaml>=6.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
]

[project.scripts]
svcctl = "apr.cli.app:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/apr"]

[tool.hatch.build.targets.wheel.sources]
"src/apr" = "apr"

# schema.sql ships inside the package tree; hatch includes it with the package.

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.25.0",
]
