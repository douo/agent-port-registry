# APR 领域上下文

本文档定义 APR 的领域语言和不可违反的权限约束。开发、运维和 Agent 在操作
APR 前必须先阅读本文档。

## 领域术语

- **节点（Node）**：一台运行独立 APR 实例的设备。每个节点拥有自己的数据库、
  服务注册表和端口分配。
- **主节点（Master Node）**：提供聚合 Web UI 的节点。它通过 SSH 读取从节点信息，
  并管理主节点本机建立的 SSH 转发。
- **从节点（Slave Node）**：被主节点通过 SSH 观测和控制运行状态的节点。P44 是
  从节点。它仍然是自身服务数据的唯一权威。
- **本地服务（Local Service）**：相对于某个 APR 实例，运行在同一节点上的服务。
  P44 服务对 P44 APR 是本地服务，对主节点只是远端快照中的服务。
- **节点快照（Node Snapshot）**：主节点通过 SSH 执行从节点只读查询后保存的缓存。
  快照用于展示，不能写回从节点，也不能并入主节点本地服务注册表。
- **运行控制（Runtime Control）**：用户在主节点明确触发后，经 SSH 对从节点已有
  服务执行 start、stop、status 或 logs。它不包括修改服务登记。
- **运行观测（Runtime Observation）**：APR 根据本机托管 PID 与服务已分配 TCP
  端口的 Listener 推导当前状态。外部 Listener 可以证明服务已运行或端口已占用，
  但不会被 APR 接管为托管进程。
- **服务自启动（Service Auto-start）**：本地服务可设置 `auto_start`。本机 APR 启动
  时，在进程管理已开启且没有托管进程或外部 Listener 的前提下执行其
  `start_command`。
- **本机转发（Local Forward）**：由主节点拥有并运行的 SSH `-L`/AutoSSH 进程。
  从节点和远端端口只是转发目标，不拥有转发记录。
- **转发规则（Forward Rule）**：主节点持久化的本机转发意图。`start`/`stop` 只控制
  当前 APR 进程期间的运行状态，`auto_start` 独立决定 APR 下次启动时是否拉起规则。
  后台监督会恢复当前应运行但父进程退出的规则。规则只判断 SSH 主机与本机监听状态，
  不以目标服务端口是否已启动作为成败条件。
- **Agent**：首次配置或重配置服务的执行者。Agent 必须在目标节点权限范围内调用
  该节点本地 APR，并把分配端口写入服务正常配置。

## 权限矩阵

| 操作 | 主节点 | 从节点本地 APR / Agent |
|---|---|---|
| 服务列表、详情、状态、日志 | 经 SSH 只读 | 权威数据源 |
| 已登记服务 start/stop | 用户明确触发后经 SSH 代理 | 实际执行并记录 |
| ensure、端口分配 | 禁止对从节点执行 | 只在目标节点本地执行 |
| 服务 create/update/delete/release | 禁止对从节点执行 | 只在目标节点本地执行 |
| 修改从节点 APR 程序、DB、配置 | 禁止 | 仅在用户明确授权该节点运维时允许 |
| SSH 本机转发 create/start/stop | 主节点本机拥有和执行 | 不保存转发记录 |

## 强制不变量

1. 每个节点的 APR 数据库只描述该节点本地服务。
2. 从节点是自身服务、端口、元数据和进程记录的唯一权威。
3. 主节点不得向从节点执行 ensure、服务 CRUD、release 或数据库写入。
4. 主节点不得把从节点快照转换为主节点本地 Service/Allocation。
5. 主节点只能通过 SSH 获得从节点信息；用户触发的启停也必须调用从节点已有
   运行控制接口，不能改写启动命令或端口。
6. 转发归主节点本机所有。记录中的目标节点关联只用于确定 SSH 路由。
7. 删除兼容代码不等于删除或重建用户已有节点数据。任何远端升级、数据库替换、
   端口迁移都需要用户对该节点的明确授权。
8. 新服务首次配置时，Agent 在目标节点向该节点本地 APR ensure；后续普通启动只用
   已持久化端口，不重复 ensure。
9. 外部启动的服务只可观测，不可由 APR stop、读取进程日志或认领为 ManagedProcess。
10. 服务自启动只作用于当前节点本地服务，不会从主节点替从节点执行自启动扫描。
11. 转发目标不是远程服务。`forward-only` 节点不提供服务登记或运行控制；只有运行
    自身 APR 的从节点，才可按既有权限代理用户明确触发的服务启停。
